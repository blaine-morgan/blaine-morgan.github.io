# Agent instructions

Static marketing site. Read [`docs/SITE.md`](docs/SITE.md) before changing copy or
layout. Do not add a build step, a framework, analytics, or third-party scripts.
Do not invent clients, numbers, testimonials or guarantees; the claims that still
need Blaine's confirmation are listed in `docs/SITE.md`. Every CTA links to the
demo funnel with `?ref=site`. Run `python3 scripts/check-docs.py` after touching
`docs/`.
